apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Pin exact image digests in prod (replace tags with @sha256:... after CI builds)
images:
  - name: ghcr.io/YOUR_ORG/streamkit-server
    newTag: "1.0.0"
  - name: ghcr.io/YOUR_ORG/streamkit-client
    newTag: "1.0.0"
